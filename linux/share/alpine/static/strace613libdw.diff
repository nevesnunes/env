diff --git a/main/strace/APKBUILD b/main/strace/APKBUILD
index 2bb7e901..2185c1a3 100644
--- a/main/strace/APKBUILD
+++ b/main/strace/APKBUILD
@@ -20,13 +20,17 @@ source="https://github.com/strace/strace/releases/download/v$pkgver/strace-$pkgv
 
 build() {
 	CFLAGS="$CFLAGS -flto=auto" \
+	LIBS="-l:libdw.a -l:libelf.a -l:libzstd.a -l:liblzma.a -l:libbz2.a -l:libz.a" \
 	./configure \
+		LDFLAGS=-static \
+		PKG_CONFIG="pkg-config --static" \
 		--build=$CBUILD \
 		--host=$CHOST \
 		--prefix=/usr \
 		--sysconfdir=/etc \
 		--mandir=/usr/share/man \
 		--localstatedir=/var \
+		--with-libdw \
 		--enable-mpers=no
 	# FIXME don't use bundled kernel headers
 	#	--enable-bundled=no
